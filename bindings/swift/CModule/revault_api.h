#ifndef REVAULT_SWIFT_SOURCE_API_H
#define REVAULT_SWIFT_SOURCE_API_H

/*
 * Source-tree bridge for `swift build`. Release assembly replaces this file
 * with the canonical standalone header so the published Swift package remains
 * self-contained.
 */
#include "../../../rust/revault_bindings/revault_api.h"

#endif
