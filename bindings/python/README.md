# reVault Python bindings

Complete class-based Python API for reVault lockboxes and vaults. Structured
results are concrete Protobuf classes transported in LBWF binary frames.
Platform wheels include the matching native Rust library.
