# APIs

The reVault APIs let applications create and manage Vaults and Lockboxes without invoking the CLI. Bindings provide the same core concepts in the conventions of each language.

Start with the [reVault API](revault-api.md) overview, then use the README and generated class or method documentation shipped with your chosen package. Those package pages are the authoritative installation reference for each language.

