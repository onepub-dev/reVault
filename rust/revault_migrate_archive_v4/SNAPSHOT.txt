Frozen source provenance

Repository: https://github.com/onepub-dev/reVault
Commit: a108240a1a0d54d4af349cd62b6a7862499e6c31
Native lockbox/container version: 4
Vault logical structure version: 3

legacy/src is an exact copy of rust/revault_lockbox_api/src at that commit.
legacy_vault/src is an exact copy of rust/revault_vault_api/src at that commit.
Both LICENSE files are copied from their source crates. The manifests rename
the packages, retain original library names for source compatibility, disable
snapshot unit/doc tests, and redirect the vault dependency to its frozen
archive reader. The shared revault_page_api supplies secure memory, not native
archive decoding. Do not update frozen source when changing the current format.

Exporter scaffold comes from revault_migrate_archive_v3 at the same commit.
The archive exporter emits migration schema 3 with source native version 4 and
target native version 5. The vault exporter preserves logical structure 3 and
advertises container version 4 separately. The current archive writer still
writes v4 at this checkpoint; this exporter is staged for the upcoming format.

Verified locally: source trees compare byte-for-byte with the committed source;
exporter and binaries compile; capability outputs distinguish structure and
container versions; signed/unsigned plaintext and encrypted/Zstd multi-frame
exports reproduce committed file bytes; source bytes remain unchanged; an
existing output artifact cannot be overwritten. These are exporter API tests,
not a complete public CLI migration lifecycle or a v4-to-v5 import test.

Release tooling knows the package paths and publishes archive reader, vault
reader, then exporter. No packages have been published by this work. CLI
selection, release build/CI coverage, full vault state equivalence, new-format
import and end-to-end migration verification remain to be completed.
