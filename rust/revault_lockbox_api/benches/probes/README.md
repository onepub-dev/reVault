# Manual probes

These programs are exploratory compression and performance probes used while
tuning the lockbox archive format. They are kept under `benches/probes` so the
crate's `examples/` directory only contains user-facing API examples.

The Criterion benchmark entry point remains `benches/performance.rs`.
